<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Manifest Path
    |--------------------------------------------------------------------------
    |
    | This value determines the path to your Vite manifest file. This is used
    | to locate the hashed filenames of your compiled assets.
    |
    */

    //'manifest_path' => env('VITE_MANIFEST_PATH', public_path('build/manifest.json')),

    /*
    |--------------------------------------------------------------------------
    | Dev Server URL
    |--------------------------------------------------------------------------
    |
    | When in development mode, the dev server will serve the assets. If this
    | is the case, this URL will be used to access the dev server.
    |
    */

    //'dev_server_url' => env('VITE_DEV_SERVER_URL', 'http://localhost:5173'),

];
